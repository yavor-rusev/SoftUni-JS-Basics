function clock() {
    let hour = 0;
    let minutes = 0;

    while (hour < 24) {
        console.log(`${hour}:${minutes}`);
        minutes++;

        if (minutes === 60) {
            minutes = 0;
            hour++;
        }
    }
}

clock();